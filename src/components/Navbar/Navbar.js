import Logo from '../Logo/Logo';
import './Navbar.scss';

const Navbar = () => {
    return (
        <div className='navbar'>
          <Logo/>
          
        </div>
    );
};

export default Navbar;